name: hprofile Actions

on: workflow_dispatch

jobs:
 Testing:
    runs-on: ubuntu-latest
    steps:
        - name: code checkout
          uses: actions/checkout@v4

        - name: Do mvn test
          run: mvn test

        - name: Do mvn checkstyle
          run: mvn checkstyle:checkstyle
          